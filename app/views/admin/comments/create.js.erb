$('.comments').prepend($('<%= j render('comments/comment', comment: @comment) %>').fadeIn('fast'))
$('#comment_content').val('')
$('#comment_content').focus()
$('#comment-count').html('<%= j render partial: "comments/comment_header" %>')
