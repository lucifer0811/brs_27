<h1><%= t "title.all_categories" %></h1>
  <% if current_user.is_admin? %>
    <%= link_to t("categories.add"), new_category_path, class: "btn btn-info" %>
  <% end %>
  <%= render "category" %>
<%= will_paginate %>
