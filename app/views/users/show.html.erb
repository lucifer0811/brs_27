<%= t "user.imformation" %>
  <h1><%= @user.name %></h1>
    <section class="stats">
      <%= render "shared/stats" %>
    </section>
  <div class="col-md-8">
      <%= render "follow_form" if logged_in? %>
  </div>
